{
  "dependencies": {
    "@prisma/client": "^6.16.0",
    "@better-auth/prisma-adapter": "1.5.0"
  },
  "devDependencies": {
    "prisma": "^6.16.0"
  },
  "scripts": {
    "migration:generate": "prisma generate",
    "migration:push": "prisma db push",
    "migration:studio": "prisma studio"
  }
}
